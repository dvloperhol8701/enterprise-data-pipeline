# enterprise-data-pipeline
Production-grade ETL pipeline using Python, SQL, &amp; Pandas. Parses unstructured server logs via Regex, structures payloads into JSON, and utilizes NumPy vectorized operations for sub-millisecond anomaly detection. Generates multi-format reporting dashboards and features an automated unittest reliability framework.
